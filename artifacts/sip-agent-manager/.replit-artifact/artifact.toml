kind = "web"
previewPath = "/"
title = "AIO Voice Connect Manager"
version = "1.0.0"
id = "artifacts/sip-agent-manager"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23208

[services.development]
run = "pnpm --filter @workspace/aio-voice-connect-manager run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/aio-voice-connect-manager", "run", "build" ]
serve = "static"
publicDir = "artifacts/sip-agent-manager/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23208"
BASE_PATH = "/"
